import {render} from 'react-dom'
import {App} from './app'

// (oq eu quero renderizar ou exibir em tela) {Dentro de qual elemento eu quero renderizar essa info}
render(
  <App />, document.getElementById('root'))
